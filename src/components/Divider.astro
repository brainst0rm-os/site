
<div class="divider" role="separator" aria-hidden="true">
	<span class="line"></span>
	<svg width="10" height="10" viewBox="0 0 10 10" aria-hidden="true">
		<polygon points="5,0 10,5 5,10 0,5" fill="currentColor" opacity="0.45"></polygon>
	</svg>
	<span class="line"></span>
</div>

<style>
	.divider {
		display: flex;
		align-items: center;
		gap: var(--space-3);
		color: var(--paper);
		width: 100%;
		max-width: 320px;
		margin-inline: auto;
	}
	.line {
		flex: 1;
		height: 1px;
		background: var(--hairline);
	}
</style>
